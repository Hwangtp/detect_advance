# 09_IO_1.py

<div style="background: linear-gradient(135deg, #f59e0b, #f97316); color: #ffffff; padding: 18px 22px; border-radius: 16px; margin: 18px 0;">
  <h2 style="margin: 0 0 8px 0;">입출력 1</h2>
  <p style="margin: 0; line-height: 1.7;">기본 입력과 출력의 흐름을 다루는 파일입니다.</p>
</div>

<table>
  <tr>
    <td style="background-color:#fff7ed; border:1px solid #fdba74; border-radius:14px; padding:16px;">
      <strong style="color:#c2410c;">왜 중요한가</strong><br><br>
      프로그램이 사용자와 상호작용하는 기본 구조를 배우는 단계입니다.
    </td>
  </tr>
</table>

## 핵심 학습 포인트

<div style="background-color:#f8fafc; border:1px solid #cbd5e1; border-radius:14px; padding:16px; margin:12px 0;">
  <ul style="margin:0; padding-left:20px; line-height:1.8;">
    <li style="margin: 8px 0;"><strong style="color:#0f766e;">1.</strong> input</li>
<li style="margin: 8px 0;"><strong style="color:#0f766e;">2.</strong> print</li>
<li style="margin: 8px 0;"><strong style="color:#0f766e;">3.</strong> 형 변환</li>
<li style="margin: 8px 0;"><strong style="color:#0f766e;">4.</strong> 사용자 입력 처리</li>
  </ul>
</div>

## 추천 학습 방법

<div style="background-color:#ecfeff; border-left:8px solid #06b6d4; padding:16px 18px; border-radius:12px; margin:12px 0;">
  <ol style="margin:0; padding-left:20px; line-height:1.9;">
    <li><code>.py</code> 파일을 직접 실행해 예제 결과를 확인합니다.</li>
    <li>주석과 출력 결과를 함께 보며 코드 흐름을 따라갑니다.</li>
    <li>예제 값을 조금씩 바꿔 보며 어떤 점이 달라지는지 관찰합니다.</li>
    <li>마지막에는 비슷한 문제를 스스로 다시 작성해 봅니다.</li>
  </ol>
</div>

## 같이 보면 좋은 파일

<div style="background-color:#f5f3ff; border:1px solid #c4b5fd; border-radius:14px; padding:16px; margin:12px 0;">
  <ul style="margin:0; padding-left:20px; line-height:1.8;">
    <li><code>Overview.md</code></li>
    <li>이전 번호 파일과 다음 번호 파일</li>
    <li>같은 주제가 이어지는 후속 실습 파일</li>
  </ul>
</div>

## 코드

```python
import csv

name = input("이름을 입력하세요: ")

print(f"안녕하세요, {name}!")

age = int(input("나이를 입력하세요: "))

print(f"당신의 나이는 {age}살입니다.")

print("Hello", "World", sep="-")

print("End", end=" ")

print("Line")

with open("example.txt", "w") as f:

    f.write("Hello, File!")

f = open("example2.txt", "w")

f.write("Hello without with!")

f.close()

with open("example.txt", "r") as f:

    content = f.read()

    print(content)

f = open("example2.txt", "r")

content2 = f.read()

print(content2)

f.close()

with open("example.txt", "a") as f:

    f.write("\nNew line")

with open("example.txt", "r") as f:

    lines = f.readlines()

    for line in lines:

        print(line.strip())

with open("data.csv", "w", newline="") as csvfile:

    writer = csv.writer(csvfile)

    writer.writerow(["Name", "Age", "City"])

    writer.writerow(["Alice", 25, "Seoul"])

    writer.writerow(["Bob", 30, "Busan"])

with open("data.csv", "r") as csvfile:

    reader = csv.reader(csvfile)

    for row in reader:

        print(row)
```
